import * as React from 'react';
import { CellEditorOverrides } from '../types';

export const cellEditorOverrides: CellEditorOverrides = {
  // You can add custom cell editor overrides here if needed
  // For now, we'll leave this empty as we're focusing on the renderer with zoom functionality
};
